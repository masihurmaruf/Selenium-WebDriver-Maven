pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(url: 'https://github.com/masihurmaruf/Selenium-WebDriver-Maven.git', branch: 'master', changelog: true, poll: true)
      }
    }
  }
}